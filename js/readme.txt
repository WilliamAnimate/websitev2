colorblindToggle.min.js - as small as possible, will not maintain behaviour if ';' is required everywhere.

colorblindToggle.js - will always work as long as somebody doesn't change how js interacts with the webpage